{
  "name": "orbits-relay-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal WebSocket relay server for the TK Messenger / Orbits encrypted text fallback (RELAY_URL). Dumb router — never inspects, logs, or mutates frame content.",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "test:hub": "node --test test/hub.test.js",
    "smoke": "node --test test/smoke.test.js",
    "check": "node --check src/config.js && node --check src/peer-id.js && node --check src/register-blob.js && node --check src/verify.js && node --check src/hub.js && node --check src/server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
