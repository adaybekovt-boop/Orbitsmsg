// Fleet / mailbox / push deployment truth. Keep these false until ops
// actually runs the public set. Tests and docs must not claim otherwise.

const bool kLiveStorageFleet = false;
const int kFleetMinBootstrap = 3;
const int kFleetMinRelay = 2;
const int kFleetMinStorage = 2;
